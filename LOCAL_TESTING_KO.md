# 로컬 테스트 안내

이 프로젝트는 배포하지 않고 개인 PC의 브라우저에서 실행할 수 있습니다.

## Windows에서 가장 쉽게 실행

1. [Node.js](https://nodejs.org/ko/download) 22.13 이상을 설치합니다.
2. 받은 ZIP을 원하는 폴더에 **전체 압축 해제**합니다.
3. 프로젝트 폴더의 `ShatteredWebDungeon-Local.exe`를 더블클릭합니다.
4. 실행기는 ZIP에 포함된 압축 게임을 바로 열며, npm 패키지나 개발 도구를
   추가로 설치하지 않습니다. 인터넷 연결과 터미널도 필요하지 않습니다.
5. 별도의 실행 중 안내창은 뜨지 않습니다. 게임 탭을 닫으면 작은 로컬 서버도
   자동으로 종료됩니다.

실행할 때마다 비어 있는 로컬 전용 포트를 골라 새 서버를 시작합니다. 이전
버전의 실행 창이나 다른 개발 서버가 남아 있어도 그 서버를 새 게임으로 잘못
열지 않으며, 지도·스프라이트 캐시도 이전 압축본에서 재사용하지 않습니다.

Windows SmartScreen이 경고할 수 있습니다. 이 실행기는 유료 코드 서명을 하지
않은 오픈소스 빌드이기 때문입니다. 내려받은 ZIP이 이 프로젝트에서 제공된
파일인지 확인한 뒤 `추가 정보` → `실행`을 선택할 수 있습니다. 실행기 소스는
`tools/windows-launcher/launcher.c`에 포함되어 있습니다.

> ZIP 파일 내부에서 EXE를 바로 실행하면 다른 프로젝트 파일을 찾지 못합니다.
> 반드시 ZIP 전체를 먼저 압축 해제해 주세요.

## 이전 버전에서 커진 용량 정리

이전 실행기는 최초 실행 때 개발용 `node_modules`를 설치해 약 800MB 이상을
사용할 수 있었습니다. 새 실행기는 이 폴더를 사용하지 않습니다. 이전 ZIP을
사용한 폴더에 `node_modules`, `.sites-runtime`, `dist` 폴더가 남아 있다면
게임을 모두 종료한 뒤 삭제해도 됩니다. 새 ZIP을 별도 폴더에 압축 해제하는
방법이 가장 간단합니다.

## 소스 개발용 터미널 실행

아래 과정은 게임을 플레이하기 위한 방법이 아니라 소스 코드를 직접 수정할
개발자에게만 필요합니다.

1. Node.js 22.13 이상을 설치합니다.
2. 압축을 푼 프로젝트 폴더에서 터미널을 엽니다.
3. 아래 명령을 실행합니다.

```bash
npm ci
npm run dev:local
```

브라우저에서 `http://127.0.0.1:5173`을 엽니다. 터미널에 다른 포트가
표시되면 그 주소를 사용합니다.

### 다음 실행부터

`package-lock.json`이 바뀌지 않았다면 의존성을 다시 설치할 필요가 없습니다.

```bash
npm run dev:local
```

개발 서버는 파일 변경을 감지해 화면을 자동 갱신합니다. 종료할 때는 실행 중인
터미널에서 `Ctrl+C`를 누릅니다.

## 최신 ZIP을 기존 작업 폴더에 반영

작업 폴더를 지우고 다시 체크아웃할 필요가 없습니다. Linux/WSL 개발 환경에서는
검증된 새 ZIP을 받은 뒤 다음 명령으로 소스만 덮어쓸 수 있습니다.

```bash
bash scripts/restore-source-overlay.sh /받은/파일/shattered-web-dungeon-local-latest.zip
```

이 명령은 ZIP을 먼저 검사하고 Git 기록, `node_modules`, 프로젝트 전용 npm
캐시를 보존합니다. `package-lock.json`, Node.js 버전, 운영체제가 기존 설치
기록과 같으면 아래 명령도 즉시 끝나며 `npm ci`나 네트워크 작업을 반복하지
않습니다.

```bash
npm run install:ci
```

## 개발자용 빠른 검증

개발 중에는 아래 명령으로 코드 규칙, 게임 로직, 구조적 공유와 성능 회귀만
확인합니다.

```bash
npm run test:quick
```

작업을 모두 마친 뒤에는 아래 명령을 **한 번만** 실행합니다. 로컬 번들,
프로덕션 빌드, 결과물 검사까지 중복 없이 순서대로 수행합니다.

```bash
npm run verify:local
```

일반 로컬 실행기에 들어갈 작은 게임 묶음은 다음 명령으로 다시 만들 수 있습니다.

```bash
npm run build:local
```

## 권장 작업 방식

- 하나의 따뜻한 체크아웃을 기준본으로 유지하고 매 작업마다 지우거나 다시
  체크아웃하지 않습니다.
- `package-lock.json`이 바뀐 경우에만 `npm ci`를 다시 실행합니다.
- 개발 중에는 실행 중인 로컬 서버와 빠른 검증을 재사용합니다.
- 같은 테스트와 빌드를 수정 전후에 중복 실행하지 않고, 수정 묶음마다 빠른
  검증 한 번과 완료 시 전체 검증 한 번만 실행합니다.
- 여러 수정 요청을 한 번에 구현하고 검증한 뒤, 명시적으로 요청한 경우에만
  원격 버전을 저장하거나 배포합니다.
- 작업 공간은 서비스 정책에 따라 정리될 수 있으므로, 검증된 커밋과 같은
  Library ZIP을 영구 복구 기준으로 사용합니다.
