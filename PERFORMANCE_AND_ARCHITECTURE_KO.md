# 규칙·표현 계층과 성능 구조

## 의존 방향

프로젝트의 실행 경계는 다음 방향만 허용합니다.

```text
React 화면 → 게임 세션/규칙
React 화면 → 표현 계층 → 읽기 전용 게임 타입
게임 규칙 ↛ React/DOM/Canvas/오디오/표현 타이밍/화면 자산
```

- `app/game/`은 상태 전이, 전투, 맵, 동료 AI, 장비·창고 트랜잭션을
  담당합니다.
- `app/presentation/`은 Canvas 렌더러, 카메라 좌표, 안개, 픽셀 효과,
  애니메이션 시간, 스프라이트 메타데이터와 공통 설명창을 담당합니다.
- `app/game/session.ts`가 한 행동 뒤의 적 턴 수와 층 전환 결과를 끝까지
  계산합니다. 화면은 결과의 표시 시점만 조절합니다.
- 전투 피드백은 `damage`, `blocked`, `defeat` 같은 의미 종류를 가집니다.
  화면은 `-12`, `처치!` 같은 번역 문구를 규칙 신호로 역해석하지 않습니다.
- `scripts/architecture-smoke.ts`가 규칙 계층의 React·브라우저·Canvas 의존,
  암묵적 시각/난수 API 사용, 로컬 import 순환과 표시 문자열 판독을 차단합니다.

## 상태 갱신

- `GameState.tiles`는 지형이나 시야가 바뀌는 행동에서만 복제합니다.
- 전투, 줍기, 장비, 인벤토리, 퀵슬롯처럼 타일을 바꾸지 않는 행동은 기존
  타일 배열을 구조적으로 공유합니다.
- 애니메이션 시작 화면은 이미 불변인 입력 상태를 그대로 사용하며, 같은
  상태 전체를 한 번 더 복제하지 않습니다.
- 새 행동을 추가할 때 타일을 직접 수정하지 않습니다. 타일/FOV 변경이 있으면
  전체 복제 경로를, 없으면 `cloneGameWithoutTiles` 경로를 사용합니다.

## 캔버스 렌더링

- `app/presentation/render-cache.ts`가 지형 프레임, 벽 오버레이, 시야 마스크를
  숫자형 배열에 저장합니다.
- 지형 키와 시야 마스크를 별도로 비교합니다. FOV만 달라지면 안개 마스크만
  갱신하고, 변하지 않은 지형 아틀라스·벽 오버레이 계산은 재사용합니다.
- 적과 동료 정렬은 해당 배열이 바뀔 때 한 번만 하며, 아이템·오브젝트·장판·적은
  카메라 밖에서 그리지 않습니다.
- `app/presentation/dungeon-renderer.ts`는 React와 분리된 Canvas 런타임입니다.
  공용 `viewport.ts`를 입력 판정과 함께 사용하므로 카메라·스프라이트 좌표를
  화면과 입력 코드가 따로 재구현하지 않습니다.
- 만료 애니메이션은 `retainInPlace`로 제자리 정리합니다. 픽셀 효과는 프레임당
  한 번만 `ground`·`actor`·`overlay` 버킷으로 분류하고 각 효과를 한 번 그립니다.
- 픽셀 안개 텍스처는 메인 캔버스에 바로 합성합니다. 매 프레임 화면 크기의
  중간 캔버스를 채우고 복사하지 않습니다.
- 움직임과 전환 중에는 최대 60fps, 물·안개·대기 애니메이션만 남으면
  30fps로 그려 CPU/GPU 사용량을 줄입니다.

## 동료 확장 구조

- `app/game/companions.ts`는 직업별 능력치·특성·장비 보너스만 정의합니다.
  원작 12×15 프레임, 스프라이트 파일, 갑옷 행은
  `app/presentation/companion-visuals.ts`에 따로 있습니다. 새 직업을 추가할 때
  규칙 정의와 시각 정의가 같은 ID로 연결되며 어느 계층도 역방향 import를
  만들지 않습니다.
- 동료 장비는 플레이어 인벤토리와 소유권을 공유하며 복제되지 않습니다.
  무기·갑옷·반지·지팡이·투척물은 모두 개별 인스턴스를 보존합니다. 퀵슬롯에
  들어간 고유 장비는 가방 슬롯에서 제외되고, 같은 종류의 투척물도 충전·내구도와
  강화 상태를 합치지 않습니다.
- 추종 동료는 플레이어의 실제 이동 흔적(`companionTrail`)을 순서대로
  사용합니다. 동료 위치는 경로 차단 집합에 넣지 않아 좁은 복도에서도
  서로 막지 않고 줄을 유지합니다. 한 턴의 최종 목적지는 먼저 예약하므로
  두 동료가 같은 칸을 선택하면 선착순 한 명만 이동하고 나머지는 대기합니다.
- 동료 동행 명령은 다른 생존 동료의 이동을 뒤따르며, 선두가 비운 칸으로
  연속 이동할 수 있습니다. 닫힌 일반 문은 별도 상호작용 턴 없이 이동과
  동시에 열고, 마지막 동료가 문칸을 벗어나면 다시 닫습니다.
- 단독 탐사는 발견된 통행 가능 타일 중 미발견 구역과 맞닿은 가장 가까운
  경계를 선택합니다. 여러 탐사자는 방향 유사도와 목표 간 거리를 함께
  평가해 서로 다른 경계를 맡습니다. 매 프레임이 아니라 동료 턴에만 목표를
  다시 계산합니다.
- 플레이어 FOV를 한 번 초기화한 뒤 생존 동료의 FOV를 누적 합성하므로,
  동료 시야 때문에 기존 시야를 반복 초기화하지 않습니다.

## 스킬·파티클 확장 구조

- `app/game/companion-skill-blueprints.ts`는 20개 스킬의 이동 방식, 충돌 방식,
  기계적 태그와 수치만 정의합니다. `deriveCompanionSkill`에 강화 모듈을
  합성하면 저장된 기본 스킬 ID를 바꾸지 않고 강화·변형 스킬을 만들 수 있습니다.
  모듈의 추가 피해·상태·회복 효과도 데이터로 정의되며 실제 스킬 실행기가
  공통 처리하므로 새 파생마다 거대한 조건문을 추가할 필요가 없습니다.
- 규칙 계층은 `leap`, `teleport`, `charge`라는 의미 신호만 보냅니다. 표현 계층은
  도약의 포물선과 착지, 순간이동의 소멸·재출현, 돌진의 지상 이동을 각각
  렌더링하므로 규칙 코드가 Canvas 시간 계산을 알 필요가 없습니다.
- `app/presentation/pixel-particle-emitters.ts`는 충격파, 파편, 찌르기, 베기,
  도약, 순간이동을 스킬 ID와 무관한 생성기로 제공합니다. 방향 벡터와 부채꼴
  각도를 넘기면 한쪽 방향으로만 퍼지는 충격파나 파편도 같은 API로 만듭니다.
- 모든 입자는 타일을 16×16으로 나눈 논리 픽셀에 맞추며, 1~2셀 크기의 사각형과
  선·원호만 사용합니다. 스프라이트 이미지에는 의존하지 않습니다.
- `app/presentation/skill-particle-recipes.ts`가 20개 스킬을 생성기 조합에
  빠짐없이 연결합니다. 일시적인 다중 타일 충격파는 타일 경계에 잘리지 않고,
  장판처럼 지속되는 효과만 자기 타일 안에 잘립니다.
- 파생 스킬은 `travelMode`, `impactMode`, 범위, 등급, 색상과 변형 ID를 실제
  파티클 조합에 사용합니다. 투사체·도약·돌진 충돌 시점은 이동 거리와 표현
  시간에 맞춰 계산하며, 수동 스킬에는 구형 안티앨리어싱 마법 도형을 겹쳐
  그리지 않습니다.

## 성장 규칙

- 레벨은 최대 50이며 레벨업 자체는 공격·방어·명중·회피·최대 생명력을
  올리지 않습니다. 레벨업은 생명력을 회복하고 증강 선택 기회만 제공합니다.
- 초기 대비 1,000배 성장 수치는 현재 레벨 배율이 아니라 앞으로 조정할
  장비 강화·특성·증강 조합의 최종 밸런스 목표입니다.

## 회귀 방지

`npm run test:quick`은 린트, 계층 계약, 게임 회귀와 성능 계약을 순서대로
검사합니다. 게임 회귀는 생성된 실행 파일 없이 깨끗한 Git 체크아웃에서도
동작합니다. 실행 파일·로컬 번들은 최종 게이트에서 실제로 생성한 다음
`test:artifact`가 별도로 확인합니다.

`npm run test:perf`는 다음을 검사합니다.

- 캐시 결과가 기존 지형·벽·안개 계산과 동일한지
- 같은 상태와 인벤토리 상태에서 타일 캐시가 재생성되지 않는지
- FOV만 바뀌면 지형 계산 0회·시야 계산 1회인지
- 동료만 바뀌면 타일 계산 0회·동료 정렬 1회인지
- 이동 시에는 새 타일 리비전이 생기는지
- 공격과 줍기에서 타일 전체를 복제하지 않는지
- 애니메이션 배열이 새 배열 할당 없이 제자리 정리되는지
- 픽셀 효과가 레이어별 재순회 대신 한 번 버킷화되는지
- 대표 상태 갱신 시간이 비정상적으로 증가하지 않는지
- 등록된 모든 동료 직업(현재 7종)의 AI와 누적 FOV가 턴당 성능 예산을
  지키는지. 측정은 7회 중앙값을 사용합니다.
- 20개 스킬 파티클 조합을 한 번에 생성해도 개별 스킬이 256개 효과를 넘지
  않고, 전체 조합 생성 시간이 성능 예산 안에 드는지.

개발 중에는 `npm run test:quick`, 완료 시에는 `npm run verify:local`을
한 번만 실행합니다.
